import { NewDropdownMenuDirective } from './new-dropdown-menu.directive';

describe('NewDropdownMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NewDropdownMenuDirective();
    expect(directive).toBeTruthy();
  });
});
